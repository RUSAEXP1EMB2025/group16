#import "@preview/grape-suite:3.0.0": german-dates, seminar-paper
#set text(font: "Noto Serif JP", lang: "ja")

#v(1fr)
#align(center)[== 要求仕様書]
#v(1fr)

#pagebreak();

= 全体概要
== システムの概要
見ているPCのブラウザの内容によって、部屋の電気を操作する。例えば、勉強のサイトを見ていたら電気を白くし、明るくする。ホラー小説を開いていたら暗くする、など。

== 製品の機能
ユーザがページを開くと、ブラウザの拡張機能としてWebページの情報を取得しサーバにその情報を送信する。サーバは、受け取ったデータから雰囲気を数値化させた雰囲気指数を算出し、明るさ指数をNatureRemo3にリクエストし明るさの情報を取得する。その明るさ指数と雰囲気指数から明るさを上げるか下げるかを決定する。

== ターゲット層
PCのブラウザでコンテンツを消費するユーザが対象である。特に、小説をインターネットで読むヒトなどが大いに役立つ。コンテンツを消費する上で、部屋の明るさを自動で調整することで、より高い雰囲気で作品を楽しむことができると考える。


= 詳細
== 機能要求
本サービスは以下の２つに分けられる。
- ブラウザの拡張機能
  - ページ内容を読み取り、バックエンドに送信する。
- 雰囲気を算出し、NatureRemo3を操作するサーバー
  - サイトの内容とURLによって処理を分ける。例えば、小説なら受け取った文字を辞書検索し、雰囲気指数を算出してNatureRemo3を操作する。他、Netflixなどではタイトルやタグからジャンルを検索する。
  - NatureRemo3の照度センサを用いて明るさを取得し、雰囲気指数から求められる明るさを数値化し、その差分から明るさを設定する。

== 非機能要求
- ユーザーがサイトを開いたら、3s以内でデバイスに状態が反映される。
- サーバは24時間365日稼働している。
